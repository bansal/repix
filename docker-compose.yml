version: "3.8"

services:
  repix:
    build: .
    ports:
      - "3210:3210"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:3210/health"]
      interval: 30s
      retries: 3
