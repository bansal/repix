---
title: Installation
description: Get started with Repix.
navigation:
  icon: i-lucide-download
  title: Installation
seo:
  description: Install and run Repix locally.
  title: Installation - Repix
---

## Prerequisites

- Node.js 22.0.0 or higher
- npm or yarn

::steps

### Clone the repository

```bash [Terminal]
git clone https://github.com/bansal/repix.git
cd repix
```

### Install dependencies

```bash [Terminal]
npm install
```

### Configure the service

Copy the example config and customize as needed:

```bash [Terminal]
cp config.example.js config.js
# Edit config.js with your settings
```

### Start in development mode

```bash [Terminal]
npm run dev
```

The server runs at [http://localhost:3210](http://localhost:3210)

### Production build

For production deployment:

```bash [Terminal]
npm run build
npm start
```

::

## Development Scripts

| Command              | Description                              |
| -------------------- | ---------------------------------------- |
| `npm run dev`        | Start development server with hot reload |
| `npm run build`      | Build for production                     |
| `npm start`          | Start production server                  |
| `npm run type-check` | Check TypeScript types                   |
| `npm test`           | Run tests (when implemented)             |
